import { Color, FontSize, Radius, BreakPoint, Spacing } from '../tokens'
import { MonorepoTheme } from './MonorepoDefaultTheme'

export const theme: MonorepoTheme = {
  fontSize: FontSize,
  radius: Radius,
  breakPoint: BreakPoint,
  spacing: Spacing,
  color: Color
}
